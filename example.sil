def main() {
    fmt.println("Hello, world!");
}

def math .{
    def pi => 3.14;

    // first-order Taylor series approximation ;)
    def sin(val x: f32) => x;
}

def fmt .{
    def println(val s: str) {
    }
}

type Vector(x: s32, y: s32);

type Expr .{
    type Add(Expr, Expr);
    type Neg(Expr);
    type Val(s32);
}
